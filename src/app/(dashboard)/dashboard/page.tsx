import Link from 'next/link';
import DashboardClient from './_components/DashboardClient';

export default function DashboardPage() {

    return (
        <main className="h-full w-full bg-glue-50">
            <DashboardClient />
        </main>
    );
}
